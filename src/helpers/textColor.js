

export const textColor = (value) => {

    if(value === 'Alive') {
      return('text-success')
    }
    if(value === 'Dead'){
      return('text-danger')
    }
    if(value === 'unknown'){
      return('')
    }

  return (
    textColor
  )
}
