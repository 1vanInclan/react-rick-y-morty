
export const blobColor = (value) => {

    if(value == 'Alive') {
        return('blob green');            
    }
    if(value === 'Dead'){
        return('blob red')
    }
    if(value === 'unknown'){
        return('blob white')
    }
    
  return (
    blobColor
  )
}
